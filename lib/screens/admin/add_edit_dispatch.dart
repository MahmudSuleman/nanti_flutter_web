import 'package:flutter/material.dart';

class AddEditDispatch extends StatelessWidget {
  static const routeName = '/add-edit-dispatch';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
